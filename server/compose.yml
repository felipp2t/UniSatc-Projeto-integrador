services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: rootly
      POSTGRES_USER: rootly
      POSTGRES_PASSWORD: rootly
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U rootly -d rootly"]
      interval: 5s
      timeout: 5s
      retries: 10
    restart: unless-stopped

volumes:
  postgres_data:
